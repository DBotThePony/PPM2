
ppm2:
  stage: build
  when: always
  script:
    - rm -rf ./lua
    - mkdir lua
    - sh build.sh
  artifacts:
    paths:
      - lua/*

moonlint:
  allow_failure: yes
  script:
    - moonc -l moon/*

linter:
  allow_failure: yes
  dependencies:
    - ppm2
  script:
    - sh luacheck.sh
